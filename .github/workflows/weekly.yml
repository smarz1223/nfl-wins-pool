name: Weekly NFL Wins Pool data refresh on: schedule: - cron: '0 11 * * 2' workflow_dispatch: {} permissions: contents: write jobs: refresh: runs-on: ubuntu-latest steps: - uses: actions/checkout@v4 - uses: actions/setup-python@v5 with: python-version: '3.11' - name: Fetch sheet and rebuild data.json run: python fetch_data.py - name: Commit updated data run: | git config user.name "github-actions[bot]" git config user.email "github-actions[bot]@users.noreply.github.com" git add data.json weekly_snapshots.json git diff --cached --quiet || git commit -m "Weekly data refresh" git push
